<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chronie.chrysorrhoego">

    <!-- 权限声明 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- 限制权限 -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" android:requiredFeature="false" />

    <!-- 防止应用被截屏或录屏（在钱包活动中） -->
    <application
        android:name=".ChrysorrhoeGoApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChrysorrhoeGO"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true"
        android:largeHeap="false"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".ui.auth.AuthActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ChrysorrhoeGO.NoActionBar"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.wallet.WalletDashboardActivity"
            android:exported="false"
            android:label="@string/wallet_dashboard"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan"
            android:noHistory="false"
            />
        
        <!-- 交易相关活动 -->
        <activity
            android:name=".ui.transaction.TransferActivity"
            android:exported="false"
            android:launchMode="standard"
            android:windowSoftInputMode="adjustPan" />
        
        <activity
            android:name=".ui.transaction.TransactionHistoryActivity"
            android:exported="false"
            android:launchMode="standard"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ChrysorrhoeGO.NoActionBar"
            />
        
        <!-- CDK兑换活动 -->
        <activity
            android:name=".ui.cdk.CdkRedeemActivity"
            android:exported="false"
            android:launchMode="standard"
            android:screenOrientation="portrait"
            />
    </application>

</manifest>